/**
 * Signal word timing constants — dates only, no word content.
 * Word content lives exclusively in the database.
 *
 * Inputs: None (static data)
 * Outputs: Exported date constants for progress bar
 * Side Effects: None
 */

/** The earliest signal word reveal date — start of the global timer. */
export const FIRST_REVEAL_DATE = "2026-02-21T06:00:00Z";

/** The final signal word reveal date (PRISM) — end of the global timer. */
export const FINAL_REVEAL_DATE = "2026-08-13T05:00:00Z";
